"""
Multi-task model (shared encoder + heads per task). Prepared for future use.
Single-task training uses AutoModelForSequenceClassification / AutoModel etc. directly.
"""
from __future__ import annotations

# Placeholder: when task_mode=multitask, instantiate shared encoder + heads per task.
# For now, train.py uses single-task models (AutoModelForSequenceClassification, etc.).
