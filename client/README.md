# Frontend (React + TypeScript)

- **Scripts**: `npm start` (dev), `npm run build` (produção), `npm run i18n:check` (chaves i18n).
- Dev: [http://localhost:3000](http://localhost:3000). Arquitetura FSD em `src/`.
