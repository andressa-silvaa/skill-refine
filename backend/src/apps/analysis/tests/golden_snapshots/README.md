# Golden snapshots for analyze_resume() refactoring safety net (Fase 0).
# Regenerate with:
#   python manage.py compare_inference_snapshots --write-baseline
# Compare with:
#   python manage.py compare_inference_snapshots
